# StarLight Backend

Flask API for the StarLight blogging platform.

See the [main README](../README.md) for setup and deployment.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python app.py
```

## Tests

```bash
pytest tests/ -q
```
